
import { useAppContext } from '../context/AppContext'
import { IContextState, IImageData } from '../typings'


// export default function Background({children} : {JSX.Element | JSX.Element[]}) {
//   const appContext: IContextState = useAppContext();
//   const logoImage: IImageData[] | undefined = appContext.imageData.filter(imgObj => imgObj.id === 'watercolor')

//   return <div></div>
// }